{
  "name": "@milaserv/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, enums and permission keys between API and Web (API First, blueprint §4)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  }
}
